{
  "name": "@atlas/integrations",
  "version": "0.0.0",
  "private": true,
  "description": "Third-party integration clients, isolated from domain packages. Stripe (ADR-018, Sprint 5); Resend, Twilio, QuickBooks (ADR-017/ADR-019, Sprint 7). Flat single package per docs/08-engineering/project-structure.md's precedent, not a nested per-integration package — each provider is a sibling src/ subdirectory, not a separate workspace package.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "stripe": "^17.7.0",
    "resend": "^4.0.1",
    "twilio": "^5.4.0"
  },
  "devDependencies": {
    "@atlas/config": "workspace:*",
    "@typescript-eslint/eslint-plugin": "^8.11.0",
    "@typescript-eslint/parser": "^8.11.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
