/**
 * Shared component library — intentionally minimal in Sprint 0.
 *
 * Only the shadcn/ui `cn` className helper is exported so far. Actual
 * shared components (Button, Input, Card, etc.) are added starting with
 * the sprint whose UI first needs them — see
 * docs/08-engineering/project-structure.md.
 */
export { cn } from './lib/cn';
