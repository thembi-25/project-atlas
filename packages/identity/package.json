{
  "name": "@atlas/identity",
  "version": "0.0.0",
  "private": true,
  "description": "Identity & Organization domain module for Project Atlas — Users, Memberships, Roles/Permissions, Organizations, Teams, invitations. See docs/13-roadmap/sprint-1.md, docs/03-domain/users.md, docs/03-domain/organization.md. Package location decided in Sprint 1 per docs/08-engineering/project-structure.md's open question — kept separate from @atlas/auth (Supabase Auth client wiring only).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@atlas/database": "workspace:*",
    "drizzle-orm": "^0.36.1"
  },
  "devDependencies": {
    "@atlas/config": "workspace:*",
    "@types/node": "^20.16.11",
    "@typescript-eslint/eslint-plugin": "^8.11.0",
    "@typescript-eslint/parser": "^8.11.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
