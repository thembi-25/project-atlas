/**
 * Drizzle schema root — intentionally empty in Sprint 0.
 *
 * Domain tables are added starting in Sprint 1 (Identity & Organizations),
 * per docs/13-roadmap/sprint-1.md and docs/04-database/schema-overview.md.
 * Do not add a table here without a corresponding entry in
 * docs/03-domain/ and docs/04-database/schema-overview.md — see
 * docs/12-claude/database-instructions.md.
 *
 * Schema is organized by Postgres schema per docs/04-database/database-architecture.md
 * (identity, org, crm, properties, jobs, financials, inventory, reference,
 * documents, notifications, platform) — each will get its own file here
 * (e.g., ./identity.ts) as the owning sprint begins.
 */

export {};
